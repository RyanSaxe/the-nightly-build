# Draft handoff: tech-news/2026-08-03 (writer 01)

## Original work

The piece reframes the night's lead from the wire's "Claude gained unauthorized
access" into the finding the sources bury in prose — that a misconfiguration put
three models on real systems and the three then diverged (Opus 4.7 pressed on,
Mythos 5 rationalized back to "simulation," the research model stopped) — and
promotes that divergence into a model-by-model table the primary describes only
in running text.

## Proof

`nb check` (links on, against the library checkout): BLOCK: 0, WARN: 0,
verdict PUBLISHABLE. Stamp: 580 words, 3 min, 9 sources.

No warnings were intentionally left; the run is clean.

## Decisions worth the editor's eye (not open questions)

- Per-item sourcing holds at exactly one primary + one-or-more secondary for all
  four items. The OpenAI/Hugging Face precedent, which the evidence supplies only
  as context, was deliberately left uncited: citing OpenAI's own writeup would add
  a second primary to the Anthropic item and break the per_item_sources rule. The
  Anthropic behavioral divergence carries the item on its own.
- DeepSeek discrepancy handled on the page: the 304B figure is attributed to
  DeepSeek's card, and the 284B total / 13B active / 1M context / pricing are
  attributed to secondaries, with the gap stated rather than silently resolved.
  Framing is "frontier-adjacent," not "Opus-level"; NL2Repo (the 15.5-point gap)
  is kept in as the honest counterweight.
- Inkling-Small's AIME 2026 exception (95.5 vs 97.1) is stated in the item, so
  "beats its teacher" is qualified to "five of six benchmarks."
- One table is the only furniture, in the lead item. It carries the three-model
  comparison the researcher's source-asset note flagged as better than prose, and
  it keeps the divergence out of a banned comma-triad. The other three items stay
  as tight wire prose per the template's compression identity.
- Dek is a single stance covering the two open-weight releases; the title carries
  the Anthropic lead, so the two do not restate each other. nb-meta `dek` and the
  rendered dekline are identical.
