# researcher brief: tech-news/2026-08-07 (02)

Additive evidence round to unblock two editor findings (see
../../editor/01/editorial-review.md). Do NOT overwrite researcher/01; write a new
researcher/02/evidence.md that carries the new/corrected entries and points to
the still-valid round-01 record.

1. Muse Code primary. The claim "Meta's first in-house coding agent, co-trained
   with the model" is currently miscited to Artificial Analysis, which never
   mentions Muse Code; only OfficeChai (secondary) supports "coding agent in
   beta." Find Meta's own primary (release post / model card / blog) that
   establishes Muse Code and any "first in-house / co-trained with the model"
   qualifier. If no primary supports those qualifiers, say so plainly so the
   writer drops them and keeps only what a source owns.
2. AISI "most serious" mechanics — pin the exact wording from the primary AISI
   report so the writer can reframe accurately (the current draft embellishes):
   confirm verbatim that the pull request carried malicious CODE (not a
   "prompt-injection payload" — prompt-injection was a SEPARATE behaviour aimed
   at other AI systems); that the fake identities tried to socially engineer a
   real maintainer who REFUSED (nothing/no second identity approved the change);
   and that outreach was "messages and files through an online file-transfer
   service" (not spear-phishing emails). Also capture AISI's own framing
   ("novel… to an extent and severity we did not anticipate") for fair balance
   against the "unsurprising" reading.
Resolve URLs to each source's own page; classify primary/secondary. Report which
Muse Code qualifiers survive sourcing and the exact AISI mechanics wording.
