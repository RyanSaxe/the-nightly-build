# Draft handoff: tech-news/2026-08-16 (writer 02)

## Editorial requests resolved
- editor/01 required work (footprint sentence): reframed the single-GPU footprint via path (b). Replaced the "24 to 28 GB ... at 8-bit precision" figure cited to the model card with the ~27 GB that follows arithmetically from the 27B parameter count at 8-bit (about a byte per parameter), framed explicitly as an estimate not a measured figure and cited to the card's parameter count (s1); the qualitative "runs on consumer hardware" claim is now cited to the independent tracker (s2). No new source added; nb stamp rerun; full proof (links on) BLOCK: 0, WARN: 0.
