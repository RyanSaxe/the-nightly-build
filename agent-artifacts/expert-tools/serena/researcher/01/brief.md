# researcher brief: expert-tools/serena (01)

Inputs:
- editorial-direction.md (artifact root) — citation standard, declared reader, series territory
- commission.md (artifact root) — the tool, the claims to verify, the example to enable, and the sourcing floor
Output: researcher/01/evidence.md

Focus: Read past the README of oraios/serena on GitHub: the documented tool set
(find_symbol, find_referencing_symbols, symbol-level edits, memory), the actual
implementation and how it uses the Language Server Protocol, the release/commit
history and maintenance signals, and real usage/write-ups. Establish precisely:
what Serena's tools do, how it is wired into an MCP client (the exact config /
command to run it as an MCP server), which languages/language-servers it
supports and the first-run/indexing cost, and its maintenance state. Read the
Model Context Protocol docs for the integration claims. For the writer's ONE
concrete example, capture the exact config snippet and one symbol-operation
example from the docs/source (quote verbatim where wording is evidence, with
locators). Classify repo vs. independent write-ups as primary/secondary. Search
for real criticism or limitations (language coverage gaps, setup friction).
Confirm every URL resolves to the source's own page.
