# Draft handoff: expert-tools/grug-far (02, revision)

## Required items resolved
1. Headline recast off the visidata "[Tool] turns [X] into [Y]" frame to a fresh
   verb pair on grug-far's own surprise — "grug-far opens a project-wide search
   as an editable buffer and writes your edits back across files" — updated
   identically in the `<title>`, nb-meta `title`, and the `<h1>`; the dek
   (validated, finer-grained) is unchanged and stays identical across nb-meta
   `dek` and the rendered dekline.
2. Heading cadence varied: of the three "clause, and clause" headings, two are
   recast to new shapes ("Why Replace runs ripgrep, not sed"; "Swapping in
   ast-grep drops the sync buffer"), leaving one comma-and heading; and two
   "not X" contrasts turned to declaratives — the heading "Editing the matches,
   not a replace field" → "Editing the matches themselves", and the orientation
   body "not a transient quickfix list but text you can change" → "is editable
   text" (the quickfix contrast is already carried earlier in that paragraph).
   The one earned contrast that is a section's whole argument (ripgrep, not sed)
   is kept.

## Preserved
All four editor direct cuts and every settled claim held; no new claims added.

## Proof
`./nb check ... --series expert-tools --library /home/user/library-checkout`
(links included): **BLOCK: 0, WARN: 0, PUBLISHABLE.** Stamped: words 1292,
reading 6 min, sources 10 (all primary).

## Open questions
None. The original-work sentence still holds and every display-text claim traces
to a primary source.
