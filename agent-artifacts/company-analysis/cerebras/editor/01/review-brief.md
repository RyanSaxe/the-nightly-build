# editor review-brief: company-analysis/cerebras (editor/01)

Inputs:
- editorial-direction.md, commission.md, and the writer's brief
  (writer/01/brief.md) — at / under the artifact root
- writing-coach/01/voice-guide.md — the register and quoted exemplars
- researcher/01/evidence.md — the evidence record (Numbers + Source assets)
- writer/01/draft-handoff.md — the draft handoff (original-work sentence; note its
  open question on the Customer-B/OpenAI inference)
- the article: library/company-analysis/cerebras.html (chart-1/2/3.py + .png sit
  beside it)
- template context under .nb-context/

Recent-pattern notes (formula check):
- Recent company pieces (SpaceX, Palantir) headline on price, open with "the
  quarter the price has to justify," run a valuation-multiple middle, and close on
  a "where the two reads part" verdict. Confirm this piece turns on the business
  question (concentration rotation, the contested margin, backlog quality) and its
  opener, section order, and closer are not those molds. Check the headline and dek
  against spec/headlines.md.

This round's focus:
1. The named-customer inference. The 10-Q labels customers A/B/C/D and never names
   them; the piece infers OpenAI is "Customer B" and the related-party pair is
   G42/MBZUAI. Verify EVERY place a customer is named is explicitly flagged as
   inference/secondary, never stated as the filing's own, and judge whether the
   argument over-rests on that inference. If any named attribution reads as the
   filing's fact, fix it in place or route it.
2. The three charts: inspect each chart-N.py provenance and rendered png. Numbers
   must match the evidence record and the cited primary; axes, scale, legend, and
   any comparison honest; caption a factual cited label. Route chart corrections to
   the writer.
3. The contested-margin core: verify the GAAP (~14.2%) and "core" (~41%) figures
   and the specific disclosed items behind the gap; confirm the piece presents both
   fairly rather than leaning on whichever flatters the argument. Recompute the
   concentration figures (88%→59%, the new customer's 20-32%) against the filing.
4. Confirm the $25.4B RPO "significant amount from the largest customer, not
   quantified per-customer" is reported as the limit it is, and that no
   earnings-call color is attributed (the transcript was inaccessible).
5. Standard: audit every data-nb-kind, open every href (the writer corrected one
   404'd URL — confirm it resolves), verify figures against owning primaries, and
   confirm the series source floor of 8 is met with real opened sources, not
   padding. Confirm there is no buy/sell/allocation call.

Proof after your edits is the orchestrator's to run (nb stamp + nb check). Route
evidence to the researcher, chart/reporting/redraft to the writer.
