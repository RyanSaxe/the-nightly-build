# Editorial review: parenting-research/infant-iron (02) — dek repair confirmed

Focused re-read of the round-01 required change. The four body findings from
round 01 stand unchanged; this pass verifies only the rewritten dek and confirms
nothing else regressed.

## Three required reads (focused)

**Skeptic:** the one broken claim from round 01 — the dek — is now factually
precise and maps to the right findings under the article's first-citation source
numbering. New dek: "Baseline iron status splits the cognitive and visual-motor
scores, a pattern that held at sixteen years, while a separate ten-year analysis
found the same infants had better adaptive behavior across the whole group, not
broken out by baseline status."

- Identical in `nb-meta` `dek` and the rendered `<p class="nb-dekline">` — they
  match verbatim.
- "baseline iron status splits the cognitive and visual-motor scores" — the
  sign-flip is now confined to cognitive/visual-motor outcomes (article s6 =
  Lozoff 2012 10-yr, evidence S6). The domain-vs-baseline conflation from round 01
  is gone.
- "a pattern that held at sixteen years" — attributed to the 16-year follow-up
  (article s7 = East 2023, evidence S8), which is neurocognition only; the body
  cites s7 exactly there, and the high-baseline-Hb-does-worst persistence is what
  the source reports. Correct.
- "a separate ten-year analysis found... better adaptive behavior across the whole
  group, not broken out by baseline status" — the adaptive-behavior benefit is
  correctly pinned to the 10-year social-emotional analysis (article s8 = Lozoff
  2014, evidence S7), no longer attributed to the 16-year study, and now explicitly
  disclaims baseline-status breakout (matching ES 0.14-0.36 across the supplemented
  group, not a subgroup split). Both round-01 errors fixed.
- Note on numbering: the article's s7 (East 16-yr) and s8 (Lozoff 2014 adaptive)
  are the reverse of the evidence record's internal S7/S8 because the article
  numbers sources in first-citation order (East is cited in the trial section
  before Lozoff 2014 in the other-outcome section). Each body claim cites the
  correct paper; the dek carries no citation numbers, so no mis-citation arises.

**Cut:** nothing new to cut. All four round-01 direct edits are intact: the
"iron-deficiency rate runs roughly triple" narrowing (orientation); the deleted
"same formula, same trial, same follow-up" closer (trial section now ends on
"small but favorable"); the deleted "a decade after the ten-year data showed the
same pattern" clause (now "performed worst overall"); and the deleted
self-referential "This desk's earlier piece on starting solids..." sentence.

**Reader:** the dek now commits to a true, sharp stance consistent with the body —
the baseline-status effect is a cognitive/visual-motor phenomenon that persisted,
and the adaptive-behavior benefit is a separate whole-group finding — and it adds
what the headline leaves out rather than restating it. It is not a comma-triad, a
semicolon reversal, or an effect-size hook.

## Untouched furniture and sourcing confirmed

- Chart (Fig. 1): `src`, alt text, caption, and s6 citation unchanged from round
  01; still all three subgroups, honest zero line/scale, labeled axes and effect
  sizes.
- Guidance table: five bodies, both axes, caption citations (s10, s11, s13, s14,
  s15) unchanged.
- Sources list s1-s15 unchanged: same URLs, same `data-nb-kind` (s13 the lone
  secondary, rest primary).
- `nb-meta` `words` refreshed to 2821 (from 2875), reflecting the ~54 words the
  round-01 cuts removed; still inside the 1200-3000 band. Writer re-proved to
  BLOCK: 0.

## Decision

Approved. The single publication-blocking item from round 01 (the dek) is
resolved, all round-01 body findings stand, and no new required change surfaced.
No redraft remains.
