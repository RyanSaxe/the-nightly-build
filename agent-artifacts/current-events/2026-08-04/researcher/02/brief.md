# researcher brief: current-events/2026-08-04 (02)

Inputs:
- ../../editorial-direction.md — citation standard (cite only what resolves and you opened)
- ../../commission.md — the brief's scope
- ../01/evidence.md — the prior evidence record (preserve still-valid work; do not overwrite it)
- ../../editor/01/editorial-review.md — the editor's two required re-sourcing items
Output: .nb-work/current-events/2026-08-04/agent-artifacts/current-events/2026-08-04/researcher/02/evidence.md

This is a targeted re-sourcing pass. The editor found two load-bearing strings cited to secondaries whose LIVE pages no longer carry them (the address a reader reaches fails). Fix exactly these; leave everything else in 01 intact and reference it.

1. Item 1 (SCOTUS mail-ballot/citizenship EO). Re-source to owning primaries and give the exact resolving URLs + verified strings:
   - The Solicitor General (Sauer) quotation "impedes the President's ability to direct his subordinates and preempts the Executive's deliberate policymaking" — find it in the stay/emergency application in SCOTUS docket 26A124 (the application PDF on supremecourt.gov), and record the exact page/locator.
   - The clause that "Justice Jackson … had called for that response" — find the docket entry/order on the 26A124 docket page showing Jackson (Circuit Justice) called for a response and the deadline. Record the resolving docket URL.
   If a primary string cannot be confirmed at a resolving URL, say so and supply the exact wording a resolving source DOES support, so the writer can file to that instead.
2. Item 2 (Blanche / anti-weaponization fund). Re-source to owning primary:
   - The rescission quotation "is rescinded and shall have no force or effect" and the order date "May 18, 2026" — from the DOJ rescission order/memo itself (justice.gov or the order document). Record the resolving URL and exact locator. If the DOJ order is not reachable at a resolving URL, supply the exact wording/date a resolving source supports and flag that the exact quote/date must be dropped or rewritten to what resolves.

For each, record in the Sources section: URL (the source's own resolving page), Kind (primary/secondary + why), Establishes, exact Paraphrase/Quote, Locators. Confirm every URL resolves (gated 403 is acceptable if it is the correct canonical page and the string is corroborated by a read source; a 404/removed page is not). Report the two resolved citations (or the fallback wording) to the orchestrator.
