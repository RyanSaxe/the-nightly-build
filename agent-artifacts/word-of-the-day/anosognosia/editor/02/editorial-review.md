# Editorial review: word-of-the-day/anosognosia (editor/02)

This is a focused second invocation to confirm the single fix routed in
editor/01: the word-card pronunciation. No re-litigation of the already-approved
piece.

## Skeptic

The lone break in editor/01 was display-text chrome: the word card printed
`\ˌa-nə-ˌsäg-ˈnō-zh(ē-)ə\` (schwa `nə` in the second syllable) where
Merriam-Webster (s1), opened firsthand, prints `nō` (macron). I confirmed the
fix against the source of record.

- **Card pronunciation, now correct.** The card reads
  `\ˌa-nō-ˌsäg-ˈnō-zh(ē-)ə\` (article line 54). Compared character by character
  against the M-W value captured verbatim in researcher/02
  (`ˌa-nō-ˌsäg-ˈnō-zh(ē-)ə`): every character matches — both stress marks
  (`ˌ`, `ˈ`), the corrected second syllable `nō`, the `(ē-)` parenthetical, and
  the terminal schwa `ə`. The only character that changed since editor/01 is the
  intended `nə` → `nō`. The card no longer contradicts the notation it imitates.
  Held.
- **Nothing else moved.** I re-checked the two editor/01 direct cuts survive:
  the Dunning-Kruger sentence carries no "overworked" adjective (lines 120-123),
  and the closing paragraph uses a period, not a semicolon
  ("...not confined to the clinic. Most of us are...", line 143). The dek, the
  six sources and their `data-nb-kind` labels, the Babinski coinage blockquote,
  the prevalence figures, and the nb-meta block (words 715, sources 6,
  reading_minutes 3, harness/model unchanged) are all as approved. writer/02
  reports the fix was surgical and touched only the one syllable; the article
  bears that out.

## Cut

No new read. The piece was cut and approved in editor/01 (two direct edits, no
prompt leakage, no repeated shape); the pronunciation fix is a single-glyph
chrome correction that introduces no prose, so there is nothing new to trim and
no reason to reopen resolved objections.

## Reader

Unchanged and still holds: the piece draws one line from Babinski's 1914 stroke
sign to the modern psychiatric burden and separates anosognosia from denial and
from Dunning-Kruger on a cited property of each. The fix corrects a fact on the
most visible furniture without altering that synthesis.

## Edits

- None. The single required change was markup (card pronunciation), outside the
  editor's remit and already made correctly by writer/02. No prose or structure
  edits were needed this round.

## Required work

- None. The editor/01 finding is resolved; writer/02 re-ran the proof to
  BLOCK 0 / WARN 0 (PUBLISHABLE) after the change.

## Decision

approve, because the word card now prints exactly the Merriam-Webster
pronunciation `\ˌa-nō-ˌsäg-ˈnō-zh(ē-)ə\` its source shows, and everything else
matches the editor/01-approved state.
