# researcher brief: opinion/hack-back-authorization (02)

Inputs:
- editorial-direction.md — citation standard
- researcher/01/evidence.md — your prior record (preserve valid work)
- editor/01/editorial-review.md — the two confirmations routed to you

Output: researcher/02/evidence.md (new record preserving 01; do not overwrite)

Two targeted quotation/citation confirmations:
1. Chris Wysopal's quoted line about a private operation that "could inadvertently
   hit a hospital": the record carries it as paraphrase, and a search rendering
   shows "affect," not "hit." Open the CNN source (s1) directly and record
   Wysopal's exact verbatim wording. If CNN blocks the fetch, try a browser-style
   request; if you still cannot read the exact words, record that plainly so the
   writer drops the quotation marks and attributes it as a paraphrase.
2. Whether source s7 names the "Active Cyber Defense Certainty Act." Open s7 and
   confirm. If it does not name that Act, either record a source that does (so the
   writer can cite it) or record that the writer should generalize the reference
   to "earlier congressional proposals."

Confirm both source URLs resolve. Report which you confirmed verbatim and which
require the writer to generalize or drop quotation marks.
