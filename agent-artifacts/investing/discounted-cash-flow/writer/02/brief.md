# writer brief: investing/discounted-cash-flow (02)

Single-owner repair. Apply every required item in ../../editor/01/editorial-review.md:
1. Fix the enterprise→equity bridge terminology. Use ONE consistent term for the
   $1,844.7M operating-asset figure (PV of FCFF discounted at WACC) across prose,
   the four-line table, the chart axis label, and the chart img alt text — and
   run the net-debt bridge from it consistently. The DaimlerChrysler example
   currently subtracts GROSS debt from a cash-inclusive figure (130,915) and the
   prose calls it "the same subtraction" as the net-debt bridge — it is not.
   Reconcile it honestly (state the difference, or reframe the example) so the
   bridge is one coherent operation. Align the chart's axis label/alt to the same
   term (that markup/asset is yours to change).
2. Fix "Year six's cash is $136.05 million grown by 2.5%" — it reads as if the
   year-five figure is year six's cash; show the $139.45M result explicitly.
The editor's 2 direct edits ("about a seventh") and all settled arithmetic stand;
add no new claims and change no verified figure. Rerun the chart if its label
changed and re-inspect the image.
Output: writer/02/draft-handoff.md (one line per required item resolved).
Proof:  ./nb check .nb-work/investing/discounted-cash-flow/library/investing/discounted-cash-flow.html --series investing --library /home/user/library-checkout  (rerun to BLOCK: 0, links included; ./nb stamp).
