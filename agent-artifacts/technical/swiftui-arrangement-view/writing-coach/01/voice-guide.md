# Voice guide

Write for a technically literate SwiftUI reader who needs a usable mental model,
not a framework launch announcement. Preserve exact API spellings in code, separate
documented behavior from inference, and scope workarounds to the reported target.
Prefer concrete layout decisions over generic responsive-design advice.
